// chrome.tabs.onUpdated.addListener((tabId, changeInfo, tab) => {
//     if (changeInfo.status === "complete" && tab.url) { // Ensure the page is fully loaded
//       chrome.scripting.executeScript({
//         target: { tabId: tabId },
//         files: ["main.js"]
//       });
//     }
//   });

 